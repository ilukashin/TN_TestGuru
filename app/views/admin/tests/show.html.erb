<h1><%= @test.title %></h1>

<p><%= link_to t('.new_question'), new_admin_test_question_path(@test), class: 'badge badge-success' %></p>
<br>
<table class='table table-bordered table-striped'>
  <thead>
    <tr>
      <th scope="col"><%= t('.question') %></th>
      <th scope="col" class="text-center" colspan="3"><%= t('.actions') %></th>
    </tr>
  </thead>
  <tbody>
    <%= render @test.questions %>
  </tbody>

</table>

<%= link_to t('.back'), admin_tests_path, class: 'badge badge-secondary' %>



